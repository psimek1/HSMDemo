using HSM;
using UnityEngine;

namespace DemoApp.ThingsOnShelfGame.View
{
    public class ThingView : HSMViewComponent
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
